import {StyleSheet, Dimensions} from 'react-native';
const styles = StyleSheet.create({
    container: { display:'flex', width:'85%', flexDirection:'row', justifyContent:'center', alignItems:'center' },
    hamburger: {},
    verveLogo: { marginLeft:'auto', marginRight:'auto', }
})

export default styles;